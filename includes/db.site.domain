$TTL					604800
@		IN		SOA		ns.{SITE}.{DOMAIN}. root.{SITE}.{DOMAIN}. (
					2013022002	; Serial
					604800		; Refresh
					86400		; Retry
					2419200		; Expire
					604800 )	; Negative Cache TTL
;
@					IN		NS		ns.{SITE}.{DOMAIN}.
ns					IN		A		{IP}
www					IN		A		{IP}
.					IN		A		{IP}
